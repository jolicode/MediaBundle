{%- trans_default_domain 'JoliMediaSonataAdminBundle' -%}

{%- if media.fileType == 'image' -%}
    <twig:joli:Img
        media="{{ media }}"
        class="{{ className|default(null) }}"
        variation="{{ variation|default('joli_media_sonata_admin') }}"
        skipAutoDimensions="{{ skipAutoDimensions|default(false) }}"
    />
{%- else -%}
    {%- if media.filetype == 'pdf' -%}
        <i class="fas fa-file-pdf" title="{{ 'media.type.pdf'|trans }}"></i>
    {%- elseif media.filetype == 'video' %}
        <i class="fas fa-file-video" title="{{ 'media.type.video'|trans }}"></i>
    {%- elseif media.filetype == 'audio' %}
        <i class="fas fa-file-audio" title="{{ 'media.type.audio'|trans }}"></i>
    {%- elseif media.filetype == 'text' %}
        <i class="fas fa-file-alt" title="{{ 'media.type.text'|trans }}"></i>
    {%- else %}
        <i class="fas fa-file" title="{{ 'media.type.file'|trans }}"></i>
    {%- endif -%}
{%- endif -%}
