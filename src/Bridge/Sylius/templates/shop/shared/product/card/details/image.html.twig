{% set product = hookable_metadata.context.product %}

<div class="mb-4">
    <div class="overflow-auto bg-light rounded-3" style="aspect-ratio: 3/4; ">
        {{ component('joli_media_sylius:shop:main_image', { product: product, class: "w-100 h-100 object-fit-cover" }) }}
    </div>
</div>
