{%- if this.webpAlternativeSrcset is not empty -%}
    <twig:joli:Source
        srcset="{{ this.webpAlternativeSrcset }}"
        sizes="{{ this.sizes }}"
        media="{{ this.media.media }}"
        skipAutoDimensions="{{ skipAutoDimensions }}"
    />
{% elseif this.webpAlternativeSource is not null -%}
    <twig:joli:Source
        variation="{{ this.webpAlternativeSource }}"
        media="{{ this.media.media }}"
        skipAutoDimensions="{{ skipAutoDimensions }}"
    />
{% endif %}
<img
    src="{{ this.path }}"
    {{- attributes.defaults({ loading: 'lazy', decoding: 'async' }) }}
    {{- attributes.nested('img') }}
    {% if this.srcset is not empty -%}
        srcset="{{ this.srcset }}"
    {% endif %}
    {% if this.sizes is not null -%}
        sizes="{{ this.sizes }}"
    {% endif %}
    {% if this.class is not null -%}
        class="{{ this.class }}"
    {% endif %}
    {% if this.width is not null -%}
        width="{{ this.width }}"
    {% endif %}
    {% if this.height is not null -%}
        height="{{ this.height }}"
    {% endif %}
/>
